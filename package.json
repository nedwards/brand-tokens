{
  "name": "brand-tokens",
  "version": "0.0.1",
  "type": "module",
  "description": "Brand token package for managing styling",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "clean": "rm -rf dist && mkdir dist",
    "build": "yarn clean && tsc && node build.mjs",
    "prepublishOnly": "yarn build"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/react": "^19.0.8",
    "style-dictionary": "^4.3.3",
    "typescript": "^5.7.3"
  }
}
